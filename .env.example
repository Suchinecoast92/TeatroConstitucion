# Copia este archivo a `.env` y ajusta valores.
# Nunca subas `.env` a Git.

APP_ENV=local

# Base de datos (WAMP local por defecto)
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=trt_25

# Backup (opcional)
DB_BACKUP_NAME=trt_25_backup

# Mercado Pago (sandbox) — Fase 3
# MP_ACCESS_TOKEN=
# MP_PUBLIC_KEY=
# MP_WEBHOOK_SECRET=

# URL pública del sitio (cuando haya dominio)
# APP_URL=http://localhost/TeatroConstitucion
